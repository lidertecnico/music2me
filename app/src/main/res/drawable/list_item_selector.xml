<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado de reproducción o seleccionado -->
    <item android:state_selected="true" android:drawable="@color/playing_color"/>
    <item android:state_activated="true" android:drawable="@color/playing_color"/>
    <!-- Estado por defecto -->
    <item android:drawable="@android:color/transparent"/>
</selector>
